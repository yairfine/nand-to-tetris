/** Initializes a new Memory game and starts running it. */
class Main {
    function void main() {
        var SimonGame game;
        let game = SimonGame.new(100);  // Called with 100 - meaning the game will be over after a 100 shpe sequence.
        do game.LandingPage();
        do game.dispose();
        return;
    }
}
